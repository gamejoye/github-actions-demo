name: Git
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: mkdir sdk
        run: rm -rf ./sdk && mkdir sdk && touch sdk/index.js
      - name: Set up Git
        run: |
          git config --global user.name 'gamejoye'
          git config --global user.email 'gamejoye@gmail.com'
      - name: Add and Commit Sdk
        run: |
          git add ./sdk
          git commit -m "chore(sdk): sdk";
      - name: Git Status
        run: git status
      - name: Push Sdk
        run: git push https://x-access-token:${{ secrets.PERSONAL_ACCESS_TOKEN }}@github.com/gamejoye/github-actions-demo.git master
        env:
          PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}


